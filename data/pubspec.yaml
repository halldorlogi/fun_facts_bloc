name: data
description: A new Flutter package project.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=2.15.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  dio: ^4.0.4
  get_it: ^7.2.0
  injectable: ^1.5.0
  shared_preferences: ^2.0.11

  ######## Local packages ###########
  domain:
    path: ../domain
  api_client:
    path: ../api_client
  ###################################

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  injectable_generator: ^1.5.0
  build_runner: ^2.1.5

flutter:

